import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "i.pinimg.com"], // Add other domains as needed
  },
};

export default nextConfig;
